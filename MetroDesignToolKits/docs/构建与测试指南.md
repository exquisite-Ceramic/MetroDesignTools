# 构建与测试指南

**版本**: v1.1.0-beta  
**适用环境**: Windows 10/11 + .NET 8.0 SDK

---

## 1. 先决条件

### 1.1 必须安装

| 工具 | 版本要求 | 获取方式 |
|:---|:---|:---|
| .NET SDK | 8.0.x | https://dotnet.microsoft.com/download/dotnet/8.0 |
| AutoCAD | 2018 或更高版本 | 正版授权安装 |
| Git | 任意版本 | https://git-scm.com |

验证安装：
```bash
dotnet --version   # 应输出 8.0.x
```

### 1.2 AutoCAD 依赖 DLL

项目编译依赖三个 AutoCAD 托管库，**不随仓库分发，需从本机 AutoCAD 安装目录手动复制**。

**合规说明**：这三个 DLL 是 Autodesk AutoCAD 的组成部分，受 Autodesk EULA 约束，不得公开分发或提交到公开仓库。本仓库 `.gitignore` 已将其排除，`build/References/` 目录下只有说明文档，不含 DLL 文件。

| 文件 | 来源路径（AutoCAD 2024 示例） |
|:---|:---|
| `acdbmgd.dll` | `C:\Program Files\Autodesk\AutoCAD 2024\` |
| `acmgd.dll` | `C:\Program Files\Autodesk\AutoCAD 2024\` |
| `accoremgd.dll` | `C:\Program Files\Autodesk\AutoCAD 2024\` |

复制到仓库目录：
```
MetroDesignToolKits/build/References/
├── acdbmgd.dll      ← 从 AutoCAD 安装目录复制，不提交 git
├── acmgd.dll        ← 同上
├── accoremgd.dll    ← 同上
└── README.md        ← 说明文档（已提交）
```

> **注意**：这三个 DLL 仅用于编译，不随插件一起发布。

---

## 2. 标准命令集

### 2.1 还原依赖

```bash
dotnet restore MetroDesignToolKits/MetroToolKits.sln
```

### 2.2 编译

```bash
# Debug（开发用）
dotnet build MetroDesignToolKits/MetroToolKits.sln

# Release（发布用）
dotnet build MetroDesignToolKits/MetroToolKits.sln -c Release
```

预期输出：`0 个错误`（警告来自 AutoCAD DLL 版本兼容性，可忽略）

### 2.3 运行单元测试

```bash
# 运行所有可脱离 AutoCAD 的单元测试
dotnet test MetroDesignToolKits/MetroToolKits.sln --filter "FullyQualifiedName~MetroToolKits.Tests"

# 单独运行 Foundation 层测试
dotnet test MetroDesignToolKits/src/Tests/MetroToolKits.Tests.Foundation/

# 单独运行 Core 层测试
dotnet test MetroDesignToolKits/src/Tests/MetroToolKits.Tests.SectionGenerator.Core/
```

预期输出：`100 个测试，0 个失败`

### 2.4 发布（打包插件）

```bash
dotnet publish MetroDesignToolKits/src/Plugins/SectionGenerator/MetroToolKits.SectionGenerator.Plugin/ \
  -c Release \
  -o ./publish/SectionGenerator
```

发布目录将包含所有运行时依赖 DLL 和配置文件。

---

## 3. 测试范围说明

### 3.1 可脱离 AutoCAD 运行的单元测试（CI 可执行）

| 测试项目 | 测试内容 | 数量 |
|:---|:---|:---|
| `MetroToolKits.Tests.Foundation` | Foundation.Core 几何类、Foundation.Building 构件剖切、ElementTypeLoader | 49 |
| `MetroToolKits.Tests.SectionGenerator.Core` | SectionComposer、MultiFloorSectionComposer、FloorGeometryHasher、SectionSnapshot、CheckSectionUpdatesUseCase | 51 |
| **合计** | | **100** |

这些测试**不依赖 AutoCAD 运行时**，可在任何 Windows 环境（包括 CI）中执行。

### 3.2 必须在 AutoCAD 宿主内验证的集成测试（手动执行）

以下功能需要在 AutoCAD 中手动验证：

| 验证项 | 命令 | 验证方式 |
|:---|:---|:---|
| 插件加载 | `NETLOAD MetroToolKits.Bootstrap.dll` | 命令行输出"Bootstrap 已加载" |
| 剖面生成 | `GenSection` | 选择剖切线，生成剖面块，目视检查 |
| 楼层配置 | `FloorConfig` | 打开 WPF 窗口，增删楼层，保存 JSON |
| 变更检测 | `CheckSectionUpdates` | 弹出非模态对话框，显示剖面状态 |
| 图层映射 | `LayerMapping` | 打开映射窗口，应用映射 |
| 构件转换 | `ConvertRegion` | 框选区域，逐层分配类型 |

---

## 4. 一键构建脚本

项目根目录提供 `build.ps1`，封装常用操作：

```powershell
# 构建
.\MetroDesignToolKits\build.ps1 build

# 测试
.\MetroDesignToolKits\build.ps1 test

# 构建 + 测试
.\MetroDesignToolKits\build.ps1 all

# 发布
.\MetroDesignToolKits\build.ps1 publish
```

---

## 5. CI 说明

仓库配置了 GitHub Actions（`.github/workflows/ci.yml`），在每次 push 和 PR 时自动执行：

1. 还原 NuGet 依赖
2. 编译解决方案（Debug）
3. 运行所有单元测试（100 个）

> **注意**：CI 环境没有 AutoCAD，因此只运行不依赖 AutoCAD 的单元测试。集成测试需在本地 AutoCAD 环境中手动执行。

---

## 6. 常见问题

**Q: 编译报错 "找不到 acdbmgd"**  
A: 检查 `build/References/` 目录是否有三个 AutoCAD DLL，参考第 1.2 节。

**Q: 测试报错 "找不到 net8.0-windows"**  
A: 确认安装了 .NET 8.0 SDK，且系统为 Windows。

**Q: 插件加载后命令不可用**  
A: 确认 `MetroToolKits.SectionGenerator.Plugin.dll` 与 `MetroToolKits.Bootstrap.dll` 在同一目录，Bootstrap 通过文件名模式 `MetroToolKits.*.Plugin.dll` 自动扫描。
