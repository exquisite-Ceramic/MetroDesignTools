# 阶段七验收记录

**版本**: v1.1.0-beta  
**更新日期**: 2026-04-20

---

## 1. 范围

本轮验收覆盖两部分：

1. 第六阶段新增代码的架构符合性复核
2. 第七阶段要求的集成测试清单、用户文档与开发者文档交付

---

## 2. 架构复核结论

### 2.1 通过项

- 项目引用关系符合 [architecture.md](./architecture.md) 中的分层约束：
  - `SectionGenerator.App` 仅依赖 `Core` 与抽象接口
  - `SectionGenerator.Infrastructure` 实现 `App.Abstractions`
  - `SectionGenerator.Plugin` 负责命令、WPF、AutoCAD 交互
- `App/Core` 层未引入 `Autodesk.AutoCAD.*` 或 `System.Windows.*`
- 第六阶段新增的双向定位能力继续沿用“App 定义接口，Infrastructure 提供实现”的模式：
  - `ISectionReferenceRepository`
  - `IEntityNavigationService`
- 第六阶段命令入口仍全部留在 `Plugin/Commands`，未把宿主逻辑下沉到用例层

### 2.2 本轮修复项

1. 去除工具箱 UI 对 `Startup.CommandRegistry` 静态状态的直接依赖  
   处理方式：
   - `SectionToolboxControl` 改为显示静态状态文案
   - `Startup` 中删除多余的全局 `CommandRegistry` 暴露

2. 统一“楼层对齐后的剖切线”在生成与更新检测中的使用  
   处理方式：
   - 新增 `FloorSectionLineTransformer`
   - `GenerateSectionUseCase` 与 `CheckSectionUpdatesUseCase` 统一通过该帮助类计算楼层对齐后的剖切线

3. 修正识别器对剖切线范围的忽略  
   处理方式：
   - `LayerBasedElementRecognizer` 仅返回与剖切线相交的构件
   - 这样每层快照哈希与更新检测不再默认扫描整张图中的所有构件

### 2.3 当前残留限制

- `viewDepth` 目前已参与命令输入、快照存储和日志记录，但“看线/投影线”的完整几何表现仍未完全落地，仍需在 AutoCAD 宿主内做最终行为确认
- 当前机器未安装 AutoCAD 宿主，且 NuGet 还原受本机代理影响，无法在本环境完成最终集成执行

---

## 3. 阶段七交付物

### 3.1 已补充

- 用户手册：[SectionGenerator用户手册.md](./SectionGenerator用户手册.md)
- 开发者扩展指南：[SectionGenerator扩展指南.md](./SectionGenerator扩展指南.md)
- 本验收记录：本文档

### 3.2 已补充的回归测试

- `FloorSectionLineTransformerTests`
- `GenerateSectionUseCaseTests`
- `CheckSectionUpdatesUseCaseTests` 新增“按楼层对齐剖切线检测”用例

---

## 4. 集成测试清单

以下场景为阶段七要求的宿主内验收项：

| 场景 | 命令 | 验收要点 | 当前状态 |
|:---|:---|:---|:---|
| 插件加载 | `NETLOAD MetroToolKits.Bootstrap.dll` | Bootstrap 成功初始化，命令可见 | 未执行 |
| 楼层配置 | `FloorConfig` | 可新增/删除楼层、保存 JSON、设置对齐点 | 未执行 |
| 多楼层剖面生成 | `GenSection` | 按配置生成多楼层剖面块，标高/楼板线正确 | 未执行 |
| 变更检测 | `CheckSectionUpdates` | 检出过期剖面并可批量更新 | 未执行 |
| 单块更新 | `UpdateSection` | 按快照重新生成并替换旧块 | 未执行 |
| 剖面定位源构件 | `LocateSourceElement` | 选中剖面实体后跳转并高亮平面源构件 | 未执行 |
| 构件查询关联剖面 | `FindRelatedSections` | 选平面构件后列出关联剖面并可定位 | 未执行 |
| 工具箱 | `ShowToolbox` | PaletteSet 可打开、停靠、按钮可触发命令 | 未执行 |
| 图层映射 | `LayerMapping` | 可查看并应用映射 | 未执行 |
| 区域转换/恢复 | `ConvertRegion` / `RevertConversion` | 转换结果可恢复 | 未执行 |

---

## 5. 当前阻塞

### 5.1 宿主环境阻塞

- 本机未发现 `acad.exe` / `accoreconsole.exe`
- 因此阶段七手动集成场景无法在当前环境实际执行

### 5.2 构建环境阻塞

- 当前 NuGet 请求被本机代理转发到 `127.0.0.1:6984`
- 依赖还原失败，无法在本环境重新完成 `dotnet build` / `dotnet test`

---

## 6. 结论

- 从架构边界看，第六阶段代码总体符合项目既定分层要求
- 本轮已修复发现的边界耦合与“楼层对齐剖切线”一致性问题
- 第七阶段所需文档与回归测试已补齐
- AutoCAD 宿主内的最终集成验收仍需在具备宿主与正常 NuGet 环境的机器上执行
